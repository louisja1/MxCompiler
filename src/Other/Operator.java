package Other;

public class Operator {
    public enum BinaryOp {
        ADD, SUB, MUL, DIV, MOD, SHL, SHR, AND, OR, XOR
    }

    public enum ConditionOp {
        EQ, NEQ, LE, LEEQ, GR, GREQ
    }

    public enum UnaryOp {
        INC, DEC, REV, NEG
    }
}
